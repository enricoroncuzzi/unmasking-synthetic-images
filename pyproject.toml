[tool.ruff]
line-length = 100
target-version = "py310"
extend-exclude = ["training", "evaluation", "data", "models", "scripts", "experiments", "profiling.py"]

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]
